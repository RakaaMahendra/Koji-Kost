# Koji Kost Frontend

This is a minimal React + Vite frontend for the Koji Kost backend.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The frontend expects the backend API running at `http://localhost:3000/api`.
